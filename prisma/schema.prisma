generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// O banco de dados está vazio por padrão, conforme solicitado para o template.
// Adicione seus modelos abaixo.
